var array = [1,2,3,4,5,6]

var select = function(array, selectingFn) {
  var result = [];
  for ( i = 0; i > array.length; i++) {
    selectingFn(array[i]);
  }
  result.push[i]
};

console.log(select(array, math.Max));
